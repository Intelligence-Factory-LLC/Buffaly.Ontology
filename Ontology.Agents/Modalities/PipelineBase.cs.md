# PipelineBase.cs Change History

## Nullable variable fixes (2025-07-09)
- Updated several local variables to nullable when assigned from `FirstOrDefault`.
- Marked additional rowArtifact variable as nullable.
